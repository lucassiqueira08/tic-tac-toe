import React from 'react';

import ImageContainer, { Container } from './styles';

const StartGameContainer: React.FC = () => (
  <Container>
    <ImageContainer />
  </Container>
);

export default StartGameContainer;
